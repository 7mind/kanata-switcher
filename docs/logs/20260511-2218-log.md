# Session log — 2026-05-11 22:18

## User request

> Create a plan to refactor main.rs into a proper trait-based multi-file architecture. The single-file nature makes diffs by contributors unbearable.

Invoked via `/review-loop`. Scope: produce the plan (M0). Execute the refactor (M1) was not commissioned.

## Work this session

- Seeded `./tasks.md` and `./defects.md` (none existed).
- Spawned one planning subagent (Opus, Plan agent type) — produced the 15-PR plan in conversation; written to `./docs/drafts/20260511-2128-mainrs-refactor-plan.md` (the Plan agent type is read-only, so the orchestrator wrote the file from the persisted result).
- Ran four adversarial-review rounds against the plan (Opus, general-purpose). Each round produced structured defects in `./defects.md` and a fix subagent (Sonnet) applied the Suggested fixes.

## Review rounds

| Round | New defects | Major | Minor | Nit | Outcome |
|---|---|---|---|---|---|
| 1 | 17 (D01–D17) | 7 | 8 | 2 | all resolved |
| 2 | 5 (D18–D22) | 2 | 2 | — | all resolved |
| 3 | 3 (D23–D25) | 1 | 1 | 1 | all resolved |
| 4 | 0 | — | — | — | clean — inner loop closed |

Round-4 reviewer verdict: "PLAN IS CLEAN. Inner loop can close."

## Highlights of the iteration

- **D01**: round-1 reviewer caught a cycle between `backends` and `supervisor` driven by `query_focus_for_env`/`resolve_display_override_for_environment`. Fix relocated the display-override block to a top-level `src/daemon/display_override.rs`.
- **D04**: planner left `#[async_trait]` in the trait example, which violated the no-new-deps rule and silently relied on a behaviour (`Send` auto-propagation) that stable AFIT does not provide. Fix pinned the seam to RPIT-in-traits with explicit `+ Send` and added a PR-00 smoke probe.
- **D07**: planner's first-pass constant inventory missed 15 constants scattered through main.rs (SNI_*, AUTOSTART_*, GNOME_SHELL_*, DBUS_ERROR_*, etc.). Fix enumerated all of them with destinations.
- **D20**: `BackendExit` (the trait's return type) was initially placed in `supervisor/`, recreating the backends→supervisor edge in a new form. Fix moved it to `backends/mod.rs` alongside the trait.
- **D23**: the D20 fix then created a self-contradictory sequencing between PR-08 and PR-12 about when `BackendExit` actually moves. Resolved by Option C: leave it in `main.rs` (widened to `pub(crate)`) until PR-12 moves it.
- **D24**: submodule re-exports for `backends/kde/mod.rs` and `backends/wayland/mod.rs` were missing — would have broken `tests.rs` `use super::*` after PR-09/PR-11.

## Deliverables

- `./docs/drafts/20260511-2128-mainrs-refactor-plan.md` — the plan (≈520 lines, 8 sections, 16 PRs PR-00..PR-15, one new trait `FocusBackend`, reuse of two existing traits, rejection of overengineered ones).
- `./tasks.md` — M0 closed `[x]`, M1 sketched but not commissioned, rich Completed entry for PR-PLAN.
- `./defects.md` — 25 defects, all `[x]` resolved, full audit trail of the four-round review.

No source code was edited.

## Final ledger state

- `tasks.md`: M0 `[x]`, M1 `[ ]` planned (16 PRs listed, not started).
- `defects.md`: 25 entries, all resolved.

## What to do next

When the user wants to execute the refactor, start at PR-00. PR-00 itself has two mandatory smoke probes:
1. Compile `wayland_scanner::generate_interfaces!("src/protocols/cosmic-workspace-unstable-v1.xml")` inside a temporary `mod foo;` under `src/daemon/`. If it fails, amend PR-09 upfront to `concat!(env!("CARGO_MANIFEST_DIR"), …)`.
2. Compile a one-line `impl FocusBackend for Stub { fn run(self: Box<Self>, _ctx: BackendRunContext) -> impl Future<Output = …> + Send { async move { … } } }` to confirm `+ Send` propagation on the installed rustc.

Both probes feed binary go/no-go decisions for downstream PRs.
